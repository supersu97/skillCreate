# 前端需求收集器

本文件是 frontend-development-helper 的需求收集模块。

## 步骤 4：收集前端开发需求

**本步骤基于步骤 1 的推断结果，只收集必要的补充信息**

### 4.1 根据推断的修改方式收集详细需求

**修改方式为"直接描述修改"时**：

```
请详细描述要修改的内容：
- 修改目标：
- 修改要求：
- 预期效果：
```

**修改方式为"根据接口信息修改"时**：

```
请提供接口信息（二选一）：
1. 接口路径和请求方法（如：GET /api/users）
2. 接口文档位置（本地文件路径或在线链接）
```

**修改方式为"指定文件修改"时**：

```
请提供要修改的文件信息：
- 文件路径或名称（如：src/components/UserList.vue）
- 修改内容描述：
```

**修改方式为"新增组件/页面"时**：

```
请提供新增内容信息：
- 名称：
- 位置：
- 功能描述：
- 包含的内容和交互逻辑：
```

### 4.2 询问是否有UI设计图或样式要求

使用 AskUserQuestion 询问：
```javascript
{
  "questions": [{
    "question": "是否有UI设计图或样式要求？",
    "header": "UI设计",
    "multiSelect": false,
    "options": [
      {
        "label": "有设计图",
        "description": "提供设计图文件路径或截图"
      },
      {
        "label": "有样式描述",
        "description": "文字描述样式要求（颜色、布局、尺寸等）"
      },
      {
        "label": "参考现有组件",
        "description": "参考项目中已有的组件样式"
      },
      {
        "label": "无特殊要求",
        "description": "使用项目默认样式规范"
      }
    ]
  }]
}
```

**如果用户选择"有设计图"**：
```
必须使用 Read 工具读取 ui-design-handler.md 文件，然后严格按照文件中的流程执行 UI 设计图处理。

读取命令：
调用 Read 工具：
- file_path: [技能基础路径]/ui-design-handler.md

执行 ui-design-handler.md 中的所有步骤，提取设计图中的布局、颜色、尺寸等信息。
```

### 4.3 生成需求摘要

将收集到的所有信息整理为需求摘要：

```
## 前端需求摘要

### 基本信息
- 需求类型：[新增功能/修改现有功能/Bug修复/性能优化]
- 修改方式：[具体方式]

### 详细需求
[根据收集到的信息填充]

### 涉及内容
- 前端：[具体内容]
- UI设计：[如有]
```

**记录所有收集到的信息**，传递给后续步骤使用。
