---
name: "fullstack-development-helper-v2"
description: "全栈开发助手 v2（优化版）。支持前端和后端项目开发，智能推断需求，简化交互流程，提供错误处理和进度跟踪。当需要进行全栈功能开发、前后端联调或同时修改前端后端代码时调用。"
---

# 全栈开发助手 v2（优化版）

*本技能专为 Claude Code 设计，帮助开发者同时处理前端和后端项目的开发任务。*

## v2 版本改进

相比 v1 版本，v2 版本包含以下改进：

1. ✅ **智能需求推断**：减少用户交互，自动推断修改范围和类型
2. ✅ **智能缓存失效检测**：基于文件变更自动判断缓存是否有效
3. ✅ **简化项目位置收集**：优先使用当前目录，减少询问
4. ✅ **拆分规划模块**：轻量规划和深度规划分离，提高效率
5. ✅ **错误处理机制**：提供重试和回滚选项
6. ✅ **进度跟踪**：使用 TaskCreate/TaskUpdate 跟踪执行进度
7. ✅ **抽象化通用原则**：移除过于具体的案例，提高通用性
8. ✅ **优化模块加载**：减少不必要的文件读取

## 模块文件说明

本技能由多个模块文件组成：

| 模块文件 | 说明 |
|---------|------|
| [SKILL.md](SKILL.md) | 核心流程主文件（步骤 1-8） |
| [project-collector.md](project-collector.md) | 项目位置收集模块（优化版） |
| [cache-handler.md](cache-handler.md) | 缓存处理模块（智能失效检测） |
| [project-analyzer.md](project-analyzer.md) | 项目分析模块（结构和代码风格） |
| [requirement-collector.md](requirement-collector.md) | 需求收集模块（智能推断） |
| [lightweight-planning.md](lightweight-planning.md) | 轻量规划模块（修改现有功能） |
| [deep-planning.md](deep-planning.md) | 深度规划模块（新增功能） |
| [collaboration-modes.md](collaboration-modes.md) | 协同开发模式模块 |
| [frontend-executor.md](frontend-executor.md) | 前端代码执行模块 |
| [backend-executor.md](backend-executor.md) | 后端代码执行模块（优化版） |
| [ui-design-handler.md](ui-design-handler.md) | UI 设计图处理模块 |
| [error-handler.md](error-handler.md) | 错误处理模块（新增） |

## ⚠️ 模块加载规则（CRITICAL）

### 内联模块（无需 Read）
以下流程已内联到主文件，无需额外读取：
- 基础的项目检测逻辑
- 简单的需求推断逻辑

### 外部模块（需要 Read）
以下模块需要使用 Read 工具读取：
- 项目位置收集（project-collector.md）
- 缓存处理（cache-handler.md）
- 项目分析（project-analyzer.md）
- 需求收集（requirement-collector.md）
- 规划模块（lightweight-planning.md / deep-planning.md）
- 协同开发（collaboration-modes.md）
- 执行模块（frontend-executor.md / backend-executor.md）
- UI 设计图处理（ui-design-handler.md）
- 错误处理（error-handler.md）

模块文件的基础路径为本 SKILL.md 所在目录（通过技能加载时的 "Base directory for this skill" 获取）。

## 🚫 技能维护规范（CRITICAL）

**禁止生成和保留辅助文档**：
- ❌ 禁止创建 CHANGELOG.md、STEP-MIGRATION.md 等辅助文档
- ❌ 禁止创建任何用于记录变更历史、步骤对照的文档
- ✅ 只保留上述"模块文件说明"表格中列出的功能模块文件

## 功能介绍

这个技能让开发者能够：

1. **前端代码开发**：支持 Vue、React、Angular 等主流前端框架
2. **后端代码开发**：支持 Spring Boot、Django、Express 等主流后端框架
3. **前后端联调**：协调前后端接口，确保数据交互正常
4. **需求分析**：智能推断需求类型和修改范围
5. **代码生成**：根据需求同时生成前端和后端代码
6. **错误处理**：提供重试和回滚机制

## 何时调用

当你遇到以下情况时，应该调用这个技能：

- 需要开发一个涉及前端和后端的完整功能
- 需要同时修改前端和后端代码
- 前后端接口联调出现问题
- 需要新建全栈功能模块
- 需要了解现有项目的技术栈和结构

## 进度跟踪

本技能使用 TaskCreate/TaskUpdate 跟踪执行进度：

1. 技能启动时创建主任务
2. 每个步骤开始时更新任务状态
3. 用户可随时使用 TaskList 查看进度

示例进度：
```
- [✓] 步骤1：智能需求分析
- [✓] 步骤2：收集项目位置
- [→] 步骤3：分析项目结构（进行中）
- [ ] 步骤4：规划阶段
- [ ] 步骤5：执行开发任务
```

## 执行指令（CRITICAL）

### 步骤 0：创建主任务并初始化

```javascript
调用 TaskCreate 工具：
{
  "subject": "全栈开发任务",
  "description": "执行全栈开发助手 v2"
}
```

### 步骤 1：智能需求分析（优化版）

**目标**：通过一次询问获取核心信息，智能推断修改范围和类型

#### 1.1 询问用户需求

使用 AskUserQuestion 工具：

```javascript
{
  "questions": [{
    "question": "请描述你要做什么？（一句话即可）",
    "header": "需求描述"
  }]
}
```

#### 1.2 智能推断

根据用户描述，智能推断以下信息：

**修改范围推断规则**：
- 包含"前端"/"页面"/"组件"/"样式"/"UI" → 前端
- 包含"后端"/"接口"/"API"/"数据库"/"服务" → 后端
- 同时包含前后端关键词 → 全栈
- 无法判断 → 询问用户

**需求类型推断规则**：
- 包含"新增"/"添加"/"创建"/"开发" → 新增功能
- 包含"修改"/"更新"/"调整"/"改" → 修改现有功能
- 包含"bug"/"错误"/"问题"/"修复" → Bug修复
- 包含"优化"/"性能"/"重构" → 性能优化
- 无法判断 → 默认为"修改现有功能"

**修改方式推断规则**：
- 包含接口路径（如"/api/xxx"） → 根据接口信息修改
- 包含文件名或组件名 → 指定文件修改
- 包含"新增"/"创建" → 新增组件/API
- 其他 → 直接描述修改

#### 1.3 确认推断结果

显示推断结果并询问用户确认：

```
## 需求分析结果

根据你的描述，我推断：
- 修改范围：[前端/后端/全栈]
- 需求类型：[新增功能/修改现有功能/Bug修复/性能优化]
- 修改方式：[直接描述/根据接口/指定文件/新增]

是否正确？如果不正确，请说明需要调整的地方。
```

如果用户确认，继续；如果需要调整，根据用户反馈修正。

**更新任务进度**：
```javascript
调用 TaskUpdate 工具：
{
  "taskId": [主任务ID],
  "status": "in_progress",
  "description": "步骤1完成：需求分析 - [修改范围] - [需求类型]"
}
```

### 步骤 2：收集项目位置信息（优化版）

**优先检查当前工作目录**

#### 2.1 智能检测当前目录

```javascript
// 检查当前目录是否是项目根目录
调用 Glob 工具：
- path: .
- pattern: "package.json"

调用 Glob 工具：
- path: .
- pattern: "pom.xml"

调用 Glob 工具：
- path: .
- pattern: "build.gradle"
```

#### 2.2 根据检测结果处理

**情况A：当前目录是项目**

```
检测到当前目录是 [项目类型] 项目：
- 项目路径：[当前目录]
- 项目类型：[前端/后端]

是否使用当前目录作为项目路径？
```

如果用户确认，跳过项目位置收集，直接进入步骤 3。

**情况B：当前目录不是项目**

必须使用 Read 工具读取 `[技能基础路径]/project-collector.md` 文件，然后严格按照文件中的流程执行。

**更新任务进度**：
```javascript
调用 TaskUpdate 工具：
{
  "taskId": [主任务ID],
  "description": "步骤2完成：项目位置 - [项目路径]"
}
```

### 步骤 3：检查和分析项目

#### 3.1 检查项目缓存（智能失效检测）

**必须先使用 Read 工具读取 `[技能基础路径]/cache-handler.md` 文件**，按照"缓存检查流程"执行。

v2 版本的缓存检查包含智能失效检测：
- 检查关键文件（package.json, pom.xml）的最后修改时间
- 对比缓存中记录的时间
- 如果文件有变更，提示用户"项目配置已变更"
- 如果缓存版本不匹配，自动重新分析

如果缓存存在且用户选择使用缓存，跳过步骤 3.2 和 3.3，直接进入步骤 4。

#### 3.2 分析项目结构

**如果使用了缓存，跳过本步骤**

**必须先使用 Read 工具读取 `[技能基础路径]/project-analyzer.md` 文件**，按照"项目结构分析"部分执行。

#### 3.3 分析代码风格

**如果使用了缓存，跳过本步骤**

**必须先使用 Read 工具读取 `[技能基础路径]/project-analyzer.md` 文件**，按照"代码风格分析"部分执行。

生成代码风格指南，后续所有代码生成必须严格遵循。

#### 3.4 保存项目缓存

**如果完成了步骤 3.2 和 3.3（未使用缓存），必须保存缓存**

**必须先使用 Read 工具读取 `[技能基础路径]/cache-handler.md` 文件**，按照"缓存保存流程"执行。

v2 版本的缓存保存包含：
- 记录关键文件的最后修改时间
- 添加缓存版本号
- 保存项目信息和代码风格

**更新任务进度**：
```javascript
调用 TaskUpdate 工具：
{
  "taskId": [主任务ID],
  "description": "步骤3完成：项目分析 - [框架] - [代码风格]"
}
```

### 步骤 4：收集详细需求

**必须先使用 Read 工具读取 `[技能基础路径]/requirement-collector.md` 文件**，然后严格按照文件中的流程执行。

v2 版本的需求收集已经过优化：
- 基于步骤 1 的推断结果，只询问必要的补充信息
- 如果是前端修改且涉及 UI，自动调用 ui-design-handler.md

**更新任务进度**：
```javascript
调用 TaskUpdate 工具：
{
  "taskId": [主任务ID],
  "description": "步骤4完成：需求收集 - [详细需求摘要]"
}
```

### 步骤 5：规划阶段并确认修改（优化版）

**在需求收集后、代码执行前，必须执行规划阶段并确认修改方案**

#### 5.1 判断任务复杂度（自动判断）

根据以下规则自动判断任务复杂度：

**简单任务（跳过规划）**：
- 修改 1-2 个文件
- Bug 修复
- 样式调整

**中等任务（轻量规划）**：
- 修改 3-5 个文件
- 修改现有功能
- 小范围优化

**复杂任务（深度规划）**：
- 新增功能模块
- 修改 6+ 个文件
- 涉及数据库变更
- 前后端联动

#### 5.2 执行对应的规划流程

**简单任务**：
- 跳过规划，直接进入步骤 6

**中等任务**：
- 必须使用 Read 工具读取 `[技能基础路径]/lightweight-planning.md`
- 执行轻量规划流程
- 生成功能边界、技术约束、实施计划
- 用户确认后继续

**复杂任务**：
- 必须使用 Read 工具读取 `[技能基础路径]/deep-planning.md`
- 进入 Plan Mode 执行深度规划
- 生成需求分析、模块划分、技术方案、任务优先级
- 保存实施计划文档
- 用户确认后继续

**更新任务进度**：
```javascript
调用 TaskUpdate 工具：
{
  "taskId": [主任务ID],
  "description": "步骤5完成：规划阶段 - [规划类型] - 已确认"
}
```

### 步骤 6：执行开发任务

**根据步骤 1 的推断结果执行不同的流程**

#### 6.1 只修改前端 → 直接执行步骤 7

基于步骤 4 已收集的修改方式和需求，直接进入"执行前端代码修改"流程（步骤 7）

#### 6.2 只修改后端 → 直接执行步骤 8

基于步骤 4 已收集的修改方式和需求，直接进入"执行后端代码修改"流程（步骤 8）

#### 6.3 同时修改前后端 → 选择协同开发模式

**必须先使用 Read 工具读取 `[技能基础路径]/collaboration-modes.md` 文件**，然后严格按照文件中的流程执行。

该模块包含两种协同开发模式：
- **模式 A：轻量顺序模式（省 Token）**：顺序执行，不启动子 Agent
- **模式 B：Agent Team 并行模式**：创建 Agent 团队，前后端并行修改

### 步骤 7：执行前端代码修改

**必须先使用 Read 工具读取 `[技能基础路径]/frontend-executor.md` 文件**，然后严格按照文件中的流程执行。

**错误处理**：
如果执行过程中出现错误，必须使用 Read 工具读取 `[技能基础路径]/error-handler.md` 文件，按照错误处理流程执行。

**更新任务进度**：
```javascript
调用 TaskUpdate 工具：
{
  "taskId": [主任务ID],
  "description": "步骤7完成：前端代码修改 - [修改文件列表]"
}
```

### 步骤 8：执行后端代码修改

**必须先使用 Read 工具读取 `[技能基础路径]/backend-executor.md` 文件**，然后严格按照文件中的流程执行。

v2 版本的后端执行器已优化：
- 移除了过于具体的 HIS 接口案例
- 抽象为通用的数据流分析原则
- 强调追溯数据源和理解业务逻辑

**错误处理**：
如果执行过程中出现错误，必须使用 Read 工具读取 `[技能基础路径]/error-handler.md` 文件，按照错误处理流程执行。

**更新任务进度**：
```javascript
调用 TaskUpdate 工具：
{
  "taskId": [主任务ID],
  "description": "步骤8完成：后端代码修改 - [修改文件列表]"
}
```

### 步骤 9：完成并总结

**标记任务完成**：
```javascript
调用 TaskUpdate 工具：
{
  "taskId": [主任务ID],
  "status": "completed",
  "description": "全栈开发任务完成"
}
```

**生成完成摘要**：
```
## 开发任务完成

### 修改范围
- [前端/后端/全栈]

### 修改内容
- [具体修改内容摘要]

### 涉及文件
1. [文件路径] - [修改说明]
2. [文件路径] - [修改说明]

### 技术约束
- 已严格遵循项目现有代码风格
- 已遵循用户指定的修改范围

### 后续建议
- [如有需要，提供测试建议]
- [如有需要，提供部署建议]
```

## 接口文档处理流程（如有需要）

如果用户提供了接口文档，按照以下流程处理：

**情况 A：本地文件**
```
1. 读取本地接口文档文件
   调用 Read 工具：
   - file_path: [用户提供的本地文件路径]

2. 解析文档格式
   - YAML 文件：使用 YAML 解析器
   - JSON 文件（OpenAPI/Swagger）：解析 JSON 结构
   - Markdown 文件：提取接口定义
   - Postman 集合：导入 Collection 格式

3. 提取接口信息
   - 接口路径和方法
   - 请求参数和类型
   - 响应结构和状态码
   - 认证要求
```

**情况 B：在线文档链接**
```
1. 获取在线接口文档
   - 如果是公开 URL：直接获取
   - 如果需要认证：提示用户提供认证信息

2. 解析文档格式
   - Swagger/OpenAPI 2.0/3.0：解析规范
   - Markdown 文档：提取接口说明
   - HTML 页面：解析接口表格

3. 验证接口可用性
   - 测试接口连通性
   - 检查接口响应格式
```

**情况 C：暂无接口文档**
```
1. 根据需求设计接口
   - 分析前端需要的数据
   - 设计 RESTful API 结构
   - 确定请求/响应格式

2. 生成接口设计文档
   - 接口列表
   - 参数说明
   - 响应示例
   - 错误码定义
```
